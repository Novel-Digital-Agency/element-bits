/**
 * EB Heading Widget JavaScript
 */
( function( $ ) {
    'use strict';

    // Any frontend JavaScript for the widget can go here
    // Currently, all styling is handled by CSS and Elementor controls
    
    $( document ).ready( function() {
        // Initialize any interactive elements if needed
    } );

} )( jQuery );
